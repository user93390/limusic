/target
.mcp.json
# rustypipe scratch (older builds wrote these into CWD; now redirected to temp)
rustypipe_cache.json
rustypipe_reports/

# Windows build artifacts — libmpv is fetched at build time (117 MB DLL), never committed.
/src-tauri/libmpv-2.dll
/.libmpv/

# Last.fm API credentials — read by src-tauri/build.rs, never committed (public repo).
/src-tauri/lastfm.keys

# Local design notes & tooling — kept out of the public repo
/context/
/progress/
/ui/perf/
/spikes/
/.agents/
/.claude/
/CLAUDE.md
/PHASE1-PROMPT.md
/RELEASING.md
/TWEETING.md
/skills-lock.json
/plans
scripts/bench.sh
bin/
